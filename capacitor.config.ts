import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lebanesewedding.app',
  appName: 'Lebanon Wedding Planner',
  webDir: 'dist',
};

export default config;
